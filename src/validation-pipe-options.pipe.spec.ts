import { ValidationPipeOptionsPipe } from './validation-pipe-options.pipe';

describe('ValidationPipeOptionsPipe', () => {
  it('should be defined', () => {
    expect(new ValidationPipeOptionsPipe()).toBeDefined();
  });
});
